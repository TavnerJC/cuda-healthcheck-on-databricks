# CUDA Healthcheck Tool - Python Dependencies
# Requires Python 3.10 or higher

# Core dependencies
# Databricks SDK for cluster scanning
databricks-sdk>=0.18.0

# Optional: ML framework detection (installed on clusters)
# These are not required to run the tool, but are detected if present
# torch>=2.0.0
# tensorflow>=2.13.0
# cudf-cu12>=23.0.0  # or cudf-cu13 for CUDA 13.x

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
pre-commit>=3.0.0

# Web interface (optional - for future development)
flask>=3.0.0
fastapi>=0.104.0
uvicorn>=0.24.0

# Data handling
pydantic>=2.0.0







